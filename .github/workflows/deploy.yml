name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # или master, в зависимости от того, как называется ваша основная ветка

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm ci

      - name: Build 🏗️
        run: npm run build
        env:
          CI: false  # Предотвращает прерывание сборки из-за предупреждений

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build  # Папка, содержащая файлы для деплоя
          branch: gh-pages  # Ветка, в которую деплоится сайт